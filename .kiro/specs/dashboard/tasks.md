# Dashboard — Implementation Tasks

- [ ] 1. Create `CarePage` server component fetching alerts, people, and profile in parallel
- [ ] 2. Create `RosterClient` client component with view switching (dashboard, roster, alerts)
- [ ] 3. Implement `StatusDonut` with conic-gradient background and legend
- [ ] 4. Implement `BatteryBars` with 4 buckets and proportional bar heights
- [ ] 5. Implement `AlertTrend` with 7-day SVG polyline chart and open count headline
- [ ] 6. Implement seniors table with photo, name, age, status, battery, last seen, and alert badge
- [ ] 7. Implement alert summary panel with person photos, linked names, and severity badges
- [ ] 8. Implement status rollup logic in `buildGroups()` merging alert severity into person state
- [ ] 9. Implement `RosterColumns` 4-column kanban view with person cards
- [ ] 10. Implement `PersonCard` component with photo, name, age, watch battery, and status signal
- [ ] 11. Add board header with "Live monitoring" badge showing online device percentage
- [ ] 12. Add footer with status color legend
- [ ] 13. Add dashboard CSS (`.care-dashboard-*`, `.dashboard-*` classes) to `globals.css`
