# Dashboard — Implementation Tasks

- [x] 1. Create `CarePage` server component fetching alerts, people, and profile in parallel
- [x] 2. Create `RosterClient` client component with view switching (dashboard, roster, alerts)
- [x] 3. Implement `StatusDonut` with conic-gradient background and legend
- [x] 4. Implement `BatteryBars` with 4 buckets and proportional bar heights
- [x] 5. Implement `AlertTrend` with 7-day SVG polyline chart and open count headline
- [x] 6. Implement seniors table with photo, name, age, status, battery, last seen, and alert badge
- [x] 7. Implement alert summary panel with person photos, linked names, and severity badges
- [x] 8. Implement status rollup logic in `buildGroups()` merging alert severity into person state
- [x] 9. Implement `RosterColumns` 4-column kanban view with person cards
- [x] 10. Implement `PersonCard` component with photo, name, age, watch battery, and status signal
- [x] 11. Add board header with "Live monitoring" badge showing online device percentage
- [x] 12. Add footer with status color legend
- [x] 13. Add dashboard CSS (`.care-dashboard-*`, `.dashboard-*` classes) to `globals.css`
